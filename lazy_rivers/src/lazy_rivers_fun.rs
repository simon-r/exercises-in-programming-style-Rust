use regex::Regex;
use std::any::Any;
use std::collections::{HashMap, HashSet};
use std::fs;
use std::str::Chars;

// fn read_text(file_name: &String) -> impl Chars {
//     let text =fs::read_to_string(file_name).expect("Error");
//     text.chars()
// }

pub fn lazy_rivers_test(file_name: &String, file_stop_w: &String) {}
